import PublicLayout from "../components/PublicLayout"

const Throttle = () => {
    return (
        <PublicLayout>
            <div>Throttle</div>
        </PublicLayout>
    )
}

export default Throttle;